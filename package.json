{
  "name": "mir-samozanyatyh",
  "version": "3.0.0",
  "private": true,
  "description": "Мир Самозанятых — единый продукт: Web + Android + Backend + AI + CRM + Marketplace. Светлана — единый AI-оператор.",
  "type": "module",
  "workspaces": [
    "backend",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n backend,web -c blue,green \"npm run dev -w backend\" \"npm run dev -w web\"",
    "dev:backend": "npm run dev -w backend",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w backend && npm run build -w web",
    "build:backend": "npm run build -w backend",
    "build:web": "npm run build -w web",
    "test": "npm run test -w backend && npm run test -w web",
    "test:backend": "npm run test -w backend",
    "test:web": "npm run test -w web",
    "e2e": "npm run e2e -w backend",
    "lint": "echo \"lint placeholder\" && exit 0",
    "start": "npm run start -w backend",
    "migrate": "npm run migrate -w backend",
    "seed": "npm run seed -w backend",
    "setup": "npm install && npm run migrate && npm run seed"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "keywords": [
    "самозанятые",
    "svetlana",
    "crm",
    "ai",
    "marketplace",
    "нпд"
  ],
  "license": "UNLICENSED",
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
